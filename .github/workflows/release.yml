name: Create Release and Package Artifact

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Read Version from plugin.json
        id: get_version
        run: |
          VERSION=$(jq -r '.version' plugin.json)
          echo "version=v$VERSION" >> $GITHUB_OUTPUT

      - name: Read CHANGELOG.md for Release Notes
        id: read_changelog
        run: |
          if [ -f "CHANGELOG.md" ]; then
            echo "body<<{delimiter}" >> $GITHUB_OUTPUT
            cat CHANGELOG.md >> $GITHUB_OUTPUT
            echo "" >> $GITHUB_OUTPUT
            echo "{delimiter}" >> $GITHUB_OUTPUT
          else
            echo "body=Automatisch generiertes Release für Version ${{ steps.get_version.outputs.version }}" >> $GITHUB_OUTPUT
          fi

      - name: Prepare Folder and Filter Files
        run: |
          mkdir mclogcleaner
          shopt -s extglob
          mv !(mclogcleaner|CHANGELOG.md) mclogcleaner/

      - name: Zip the Folder
        run: |
          zip -r mclogcleaner.zip mclogcleaner/

      - name: Create GitHub Release and Upload Asset
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.get_version.outputs.version }}
          name: "Release ${{ steps.get_version.outputs.version }}"
          body: ${{ steps.read_changelog.outputs.body }}
          files: mclogcleaner.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
